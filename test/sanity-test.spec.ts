import {
  it,
  inject,
  injectAsync,
  beforeEachProviders,
} from '@angular/core/testing';

describe('sanity checks', () => {
  it('should also be able to test', () => {
    expect(true).toBe(true);
  });
});
